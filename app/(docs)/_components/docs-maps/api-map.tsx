import { DocumentMap } from "@/types/docs";
import { api } from "../const/api";



export const api_map: DocumentMap = {
    id: 'api',
    root: true,
    name: 'API',
    blocks: [
    ],
    side: api
}